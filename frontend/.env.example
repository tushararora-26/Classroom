# The backend's origin, with no trailing slash and no /api/v1 suffix —
# src/lib/api.js appends that itself.
#
# Local:     http://localhost:3000
# On Vercel: https://your-backend-project.vercel.app
VITE_API_URL=http://localhost:3000
